import { Button } from "@/components/ui/button";

const Dashboard = () => {
    return (
        <div>
            <p>Dashboard</p>
            <Button>Logout</Button>
        </div>
    );
};

export default Dashboard;
